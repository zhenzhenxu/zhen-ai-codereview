# Zhen AI Code Review

🤖 AI驱动的代码审查工具，使用 OpenAI 自动审查 PR 代码并发表中文评论。

[![GitHub Action](https://img.shields.io/badge/GitHub-Action-blue?logo=github)](https://github.com/zhenzhenxu/zhen-ai-codereview)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

---

## 📖 目录

- [功能介绍](#-功能介绍)
- [实现原理](#-实现原理)
- [快速开始](#-快速开始)
- [配置选项](#️-配置选项)
- [本地使用](#-本地使用)
- [项目结构](#-项目结构)

---

## ✨ 功能介绍

### 核心功能

| 功能 | 说明 |
|------|------|
| 🔍 **自动审查** | PR 创建/更新时自动触发代码审查 |
| 🇨🇳 **中文输出** | 默认输出中文审查结果，也支持英文 |
| 🔒 **安全检测** | 检测 SQL注入、XSS、命令注入等安全漏洞 |
| ⚡ **性能分析** | 识别低效算法、内存泄漏、O(n²)复杂度等问题 |
| 📝 **代码质量** | 检查命名规范、代码可读性、文档注释 |
| 🎯 **精准定位** | 引用具体行号，提供修复代码示例 |
| 💬 **自动评论** | 审查结果自动发表到 PR 评论区 |

### 审查维度

AI 会从以下 5 个维度审查代码：

| 级别 | 类别 | 检查内容 |
|------|------|----------|
| 🔴 严重 | **Bug与错误** | 逻辑错误、空指针、竞态条件、边界情况 |
| 🔴 严重 | **安全性** | SQL注入、XSS、认证问题、敏感数据泄露、命令注入 |
| 🟡 警告 | **性能** | 低效算法、内存泄漏、不必要的计算、N+1查询 |
| 🔵 建议 | **代码质量** | 可读性、可维护性、命名规范、文档注释 |
| 🔵 建议 | **最佳实践** | 设计模式、SOLID原则、错误处理、代码复用 |

### 支持的编程语言

支持所有主流编程语言：

- **前端**：JavaScript, TypeScript, Vue, React, Svelte
- **后端**：Python, Java, Go, Rust, C/C++, C#
- **移动端**：Swift, Kotlin, Objective-C
- **其他**：Ruby, PHP, Scala, Shell

---

## 🔧 实现原理

### 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        GitHub Actions                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────┐    ┌─────────────┐    ┌─────────────────────┐    │
│   │  PR     │───▶│  获取代码   │───▶│  调用 OpenAI API    │    │
│   │  事件   │    │  变更(diff) │    │  进行代码审查       │    │
│   └─────────┘    └─────────────┘    └──────────┬──────────┘    │
│                                                 │                │
│                                                 ▼                │
│                  ┌─────────────┐    ┌─────────────────────┐    │
│                  │  发表评论   │◀───│  生成审查报告       │    │
│                  │  到 PR      │    │  (Markdown格式)     │    │
│                  └─────────────┘    └─────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 工作流程

1. **触发阶段**：当 PR 创建或更新时，GitHub Actions 自动触发
2. **获取变更**：通过 GitHub API 获取 PR 中所有变更的文件和 diff 内容
3. **代码审查**：将代码变更发送给 OpenAI API，使用专业的 Prompt 进行审查
4. **生成报告**：将 AI 返回的审查结果格式化为 Markdown 报告
5. **发表评论**：通过 GitHub API 将审查报告发表到 PR 评论区

### 核心模块

```
zhen-ai-codereview/
├── src/zhen_ai_codereview/
│   ├── llm/
│   │   └── openai_client.py   # OpenAI API 封装，包含审查 Prompt
│   ├── github/
│   │   └── client.py          # GitHub API 封装，获取PR信息、发表评论
│   ├── local/
│   │   └── scanner.py         # 本地代码扫描，支持文件/目录/git变更
│   ├── agent/
│   │   └── reviewer.py        # 核心审查 Agent，协调各模块工作
│   ├── config.py              # 配置管理
│   └── main.py                # CLI 入口
├── scripts/
│   └── review_pr.py           # GitHub Action 入口脚本
└── action.yml                 # GitHub Action 配置
```

### 关键技术

| 技术 | 用途 |
|------|------|
| **OpenAI GPT-4o** | AI 代码审查引擎 |
| **PyGithub** | GitHub API 操作 |
| **GitPython** | 本地 Git 仓库操作 |
| **Click** | CLI 命令行工具 |
| **Rich** | 终端美化输出 |

### Prompt 设计

审查 Prompt 的核心设计思路：

```python
SYSTEM_PROMPT = """你是一位资深的代码审查专家，精通软件工程最佳实践、安全性和性能优化。

审查重点：
1. Bug与错误：逻辑错误、空指针问题、竞态条件、边界情况
2. 安全性：SQL注入、XSS、认证问题、敏感数据泄露
3. 性能：低效算法、内存泄漏、不必要的计算
4. 代码质量：可读性、可维护性、命名规范、文档注释
5. 最佳实践：设计模式、SOLID原则、错误处理

输出格式要求：
- 尽可能具体，引用行号
- 提供改进建议的代码示例
- 按严重程度分类：🔴 严重、🟡 警告、🔵 建议
"""
```

---

## 🚀 快速开始

### 在你的项目中启用 AI 代码审查

#### 第一步：添加 Workflow 文件

在你的项目根目录创建 `.github/workflows/code-review.yml` 文件：

```yaml
name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: zhenzhenxu/zhen-ai-codereview@main
        with:
          openai_api_key: ${{ secrets.OPENAI_API_KEY }}
```

#### 第二步：配置 Secret

1. 打开你的 GitHub 仓库
2. 进入 **Settings** → **Secrets and variables** → **Actions**
3. 点击 **New repository secret**
4. 填写：
   - **Name**: `OPENAI_API_KEY`
   - **Secret**: 你的 OpenAI API Key（如 `sk-proj-xxx...`）
5. 点击 **Add secret**

#### 第三步：完成！

现在每次创建或更新 PR 时，AI 会自动审查代码并在 PR 下发表中文评论。

---

### 一键添加脚本

在你的项目目录下执行以下命令，快速添加 AI 代码审查：

```bash
# 创建 workflow 目录
mkdir -p .github/workflows

# 创建配置文件
cat > .github/workflows/code-review.yml << 'EOF'
name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: zhenzhenxu/zhen-ai-codereview@main
        with:
          openai_api_key: ${{ secrets.OPENAI_API_KEY }}
          language: 'zh'
EOF

# 提交并推送
git add .github/workflows/code-review.yml
git commit -m "feat: 添加 AI 代码审查"
git push
```

然后去仓库 **Settings → Secrets → Actions** 添加 `OPENAI_API_KEY` 即可。

---

## ⚙️ 配置选项

### 完整配置示例

```yaml
- uses: zhenzhenxu/zhen-ai-codereview@main
  with:
    # 必填：OpenAI API Key
    openai_api_key: ${{ secrets.OPENAI_API_KEY }}

    # 可选：GitHub Token（默认自动提供）
    github_token: ${{ secrets.GITHUB_TOKEN }}

    # 可选：使用的模型（默认 gpt-4o）
    openai_model: 'gpt-4o'

    # 可选：输出语言，zh=中文，en=英文（默认 zh）
    language: 'zh'

    # 可选：是否发表评论（默认 true）
    post_comment: 'true'

    # 可选：排除的文件模式，逗号分隔
    exclude_patterns: '*.md,*.txt,*.json,*.lock,*.yml'
```

### 配置参数说明

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `openai_api_key` | ✅ | - | OpenAI API Key |
| `github_token` | ❌ | 自动提供 | GitHub Token，用于发表评论 |
| `openai_model` | ❌ | `gpt-4o` | 使用的 OpenAI 模型 |
| `language` | ❌ | `zh` | 输出语言：`zh`(中文) 或 `en`(英文) |
| `post_comment` | ❌ | `true` | 是否在 PR 上发表评论 |
| `exclude_patterns` | ❌ | `*.md,*.txt,...` | 排除不审查的文件模式 |

---

## 🖥️ 本地使用

除了 GitHub Action，你也可以在本地命令行使用：

### 安装

```bash
# 克隆项目
git clone https://github.com/zhenzhenxu/zhen-ai-codereview.git
cd zhen-ai-codereview

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
export OPENAI_API_KEY="sk-proj-your-api-key"
```

### 使用命令

```bash
# 审查单个文件
./zhen-review.sh review src/main.py

# 审查整个目录
./zhen-review.sh review src/

# 审查多个文件
./zhen-review.sh review file1.py file2.py file3.py

# 审查 git 最近一次提交的变更
./zhen-review.sh diff

# 审查两个分支之间的差异
./zhen-review.sh diff --base main --head feature-branch

# 审查暂存区的代码（提交前审查）
git add .
./zhen-review.sh staged

# 审查 GitHub PR（需要配置 GITHUB_TOKEN）
export GITHUB_TOKEN="ghp-your-token"
./zhen-review.sh pr owner/repo 123

# 审查 PR 并发表评论
./zhen-review.sh pr owner/repo 123 --comment
```

### 命令说明

| 命令 | 说明 |
|------|------|
| `review <path>` | 审查本地文件或目录 |
| `diff` | 审查 git 变更 |
| `staged` | 审查暂存区代码 |
| `pr <repo> <number>` | 审查 GitHub PR |

---

## 📁 项目结构

```
zhen-ai-codereview/
├── src/
│   └── zhen_ai_codereview/
│       ├── __init__.py          # 包初始化
│       ├── config.py            # 配置管理（环境变量、默认值）
│       ├── main.py              # CLI 命令入口
│       ├── agent/
│       │   ├── __init__.py
│       │   └── reviewer.py      # 核心审查 Agent
│       ├── llm/
│       │   ├── __init__.py
│       │   └── openai_client.py # OpenAI API 封装
│       ├── github/
│       │   ├── __init__.py
│       │   └── client.py        # GitHub API 封装
│       └── local/
│           ├── __init__.py
│           └── scanner.py       # 本地代码扫描器
├── scripts/
│   └── review_pr.py             # GitHub Action 入口脚本
├── examples/
│   └── user_service.py          # 示例代码（含故意的问题）
├── .github/
│   └── workflows/
│       └── example-usage.yml    # 使用示例
├── action.yml                   # GitHub Action 配置
├── Dockerfile                   # Docker 支持
├── pyproject.toml               # Python 项目配置
├── requirements.txt             # 依赖列表
├── zhen-review.sh               # 本地运行脚本
└── README.md                    # 说明文档
```

---

## 📄 示例审查结果

当 AI 审查代码后，会在 PR 下发表类似这样的评论：

```markdown
## 🤖 AI Code Review

**PR:** 添加用户服务模块
**Files reviewed:** 3/5

---

### Summary

整体代码质量良好，但存在以下安全隐患需要修复：
1. SQL 注入漏洞
2. 不安全的密码处理
...

---

### Detailed Review

#### src/user_service.py

### 🔴 严重：SQL注入风险（第34行）

直接将用户输入拼接到SQL语句中，存在注入风险。

**问题代码：**
```python
sql = f"SELECT * FROM users WHERE id = '{user_id}'"
```

**建议修复：**
```python
# 使用参数化查询
sql = "SELECT * FROM users WHERE id = ?"
cursor.execute(sql, (user_id,))
```

### 🟡 警告：性能问题（第56行）

使用 O(n²) 复杂度的算法检查重复...
```

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📜 License

MIT License

---

## 🔗 相关链接

- GitHub: https://github.com/zhenzhenxu/zhen-ai-codereview
- OpenAI: https://platform.openai.com/
